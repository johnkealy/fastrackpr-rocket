{% embed 'rocketship_theme_starter:cb-media' with {
  attributes,
  classes: [
    derivative_plugin_id|clean_class|trim('cb-', 'left'),
    content['#block_content'].field_cb_media_layout.value ? 'cb--view-mode--' ~ content['#block_content'].field_cb_media_layout.value
  ],
  component: 'media',
  media: content.field_cb_media,
  link: {
    url: image_wrapper_url,
    title: image_wrapper_url_title,
    target: image_wrapper_url_target,
  },
  content: content|without(
    'field_cb_media'
    'field_cb_image_wrapper_link',
    'field_cb_media_layout',
  ),
  title_suffix: title_suffix,
} %}
{% endembed %}
